---
interface Props {
	class?: string;
    id?: string;
}

const { class: className, id } = Astro.props;
---

<section id={id} class:list={["mb-[var(--space-2xl)] md:mb-[var(--space-3xl)] scroll-mt-20", className]}>
	<slot />
</section>
